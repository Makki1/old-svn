# Defaults for xpleibd initscript
# sourced by /etc/init.d/xpleibd
# installed at /etc/default/xpleibd by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Meaningful params in daemon mode:
#  -daemon   -- detach and run as daemon
#  -nosup    -- Do not run supervisor-daemon (default=yes)
#  -p <filename> -- pidfile-nae (else /var/run/xpleibd.pid
#  -interface x   -- Use interface named x (i.e. eth0) as network interface
#  -ip x  -- Bind to specified IP address for xPL
#  -u <url> - set eibd url (ie.e local:/tmp/eib or ip:127.0.0.1
#
# Additional options that are passed to the Daemon.
DAEMON_OPTS=" -daemon "
